[🇬🇧 English](#english) · [🇩🇪 Deutsch](#deutsch)

<a name="english"></a>

# Code-Crash Framework — Conventions

> **What this is.** A tool-neutral specification of the Code-Crash Framework: the file layouts, frontmatter shapes, workflow rules, and CI gates that any compliant project follows — regardless of which AI coding tool drives it (Claude Code, Codex, Cursor, Aider, a local LLM, or no AI tool at all).
>
> **Why it exists.** The framework is an operational answer to Matthias Schrader's "Code Crash" (2026): once AI takes over the act of writing code, the bottleneck moves from typing to Intent, and the work has to be re-organised around it. The conventions in this document are the pieces that make that real — they have to be tool-portable because the AI landscape shifts faster than any one tool stays optimal. For the chapter-by-chapter mapping from Schrader's book to this bundle, see **HANDBUCH Appendix M — Schrader Decoder**.

## §0. Who this document is for

You probably arrived here for one of three reasons. The document is written so each reader finds the right starting point fast.

### As an operator setting up a new project

Read §1 (project layout) and §6 (tool-portability checklist) first. Everything you need to scaffold a compliant repository is in those two sections. The other sections explain *why* the layout looks the way it does — useful when you want to deviate intentionally.

### As an operator migrating away from Claude Code (or onto it)

Read §4 (tool-agnostic components) and §5 (tool-specific components) first, then §6. About 70 % of the framework — hooks, GitHub Actions, markdown artifacts, the `journal/` tree — runs unchanged no matter which AI tool you use. The remaining 30 % (skill invocation, MCP integrations, sub-agents) is what you re-wire per tool. **HANDBUCH Appendix K — Tool-Adapter** walks through the swap step by step.

### As someone trying to understand the framework

Read §0 (this section), then §3 (workflow conventions), then **HANDBUCH Appendix M — Schrader Decoder**. The conventions in §1-§2 are mostly mechanical; the *why* lives in §3 and in the Schrader decoder. If you have never opened Schrader's book, that's fine — the decoder is a translation, not a re-reading.

### Glossary (the eight terms you need before everything else)

| Term | One-sentence definition |
|---|---|
| **Intent** | The precise, measurable outcome a user should reach — the new scarce resource since AI absorbed the cost of writing code (Schrader Ch. 4). |
| **4P pipeline** | Perceive → Prompt → Produce → Pitch. The end-to-end flow from a noticed problem to a delivered demo (Schrader Ch. 5). Each phase is wired to one or more skills (Appendix L). |
| **Skill** | A repeatable workflow Claude Code (or another AI tool) executes when invoked via slash command — `/intent`, `/implement`, `/sprint-review`, etc. Lives at `<skill>/SKILL.md`. |
| **BOO-ID** | Linear issue identifier in the Bootstrapping Evolution project. Every framework change has a BOO-ID — the conventions reference them so you can trace any rule to its origin. |
| **Spec file** | `specs/{ISSUE-ID}.md` per story. Generated by `/ideation`, blocked-on by `hooks/spec-gate.sh`, audit-trailed by `/implement`. The smallest unit of "what we are building right now". |
| **Quality gate** | Any check that must pass before code progresses — ESLint, Semgrep, coverage, performance baseline, SonarQube. Wired in three layers (IDE, pre-commit, CI). |
| **Learning loop** | L1 / L2 / L3 — markdown, dated markdown, SQLite. The framework writes lessons after every sprint and reads them before every story, so the same mistake costs less the second time. |
| **Hermes** | Optional external compound-engineering layer that consumes the bundle's CI outputs and proposes patterns across projects. Lives on a separate VPS. Skipping it is fully supported. |

## §1. Project layout

Every project that follows the Code-Crash Framework has this top-level structure. **Why this layout exists:** Schrader Ch. 5 argues the 4P pipeline replaces the classical approval process — that only works if Intent (`intents/`), execution (`specs/`), evidence (`journal/`) and outcome (`pitch/`) each have a fixed home.

```
<project-root>/
├── .claude/                              # Tool config (Claude Code; other tools mirror)
│   ├── environment.json                  # Paths, thresholds, tools_available
│   ├── sensitive-paths.json              # Mandatory-review patterns (BOO-18)
│   └── ISSUE_WRITING_GUIDELINES.md       # Story-template rules (BOO-11/27/30)
├── .github/
│   └── workflows/                        # CI gates (BOO-3/4/5/12/15/16/28/45)
│       ├── eslint.yml                    # or ruff.yml for Python
│       ├── semgrep.yml
│       ├── sonar.yml                     # only if SonarQube enabled (BOO-5)
│       ├── perf.yml                      # backend stacks (BOO-16)
│       ├── lighthouse.yml                # frontend stacks (BOO-45)
│       └── ISSUE_TEMPLATE/
│           └── story.yml                 # 4-Schrader-block template (BOO-11/27/30)
├── ARCHITECTURE_DESIGN.md                # §1-9 hub doc, single source of truth
├── CHANGELOG.md
├── CLAUDE.md                             # Project context for AI tools (also for Codex)
├── GOVERNANCE.md                         # Project-specific rules and tool stack
├── Projekt-Governance.md                 # SecondBrain-side governance (if Obsidian active)
├── README.md
├── SECURITY.md
├── bench/                                # Benchmarks per service (BOO-16)
│   └── <service>.bench.js or _bench.py
├── docs/                                 # Architecture diagrams, ADRs
├── hooks/                                # Local git hooks (NOT .git/hooks/)
│   ├── spec-gate.sh                      # BOO-23: every commit needs a spec
│   ├── doc-version-sync.sh               # Version sync across docs
│   └── dependency-check.sh               # BOO-12: hallucination check
├── intents/                              # Intent files (BOO-1)
│   └── INTENT-XX.md                      # One per intent; read by /pitch
├── journal/
│   ├── learnings.md                      # L1 (default)
│   ├── learnings/{YYYY-QX}.md            # L2 (recommended ~10+ sprints)
│   ├── learnings.db                      # L3 (recommended 50+ sprints, gitignored)
│   ├── perf-baseline.json                # Living performance baseline (BOO-16)
│   └── reports/
│       ├── local/                        # Implement-run outputs (BOO-36), gitignored
│       │   └── {YYYY-MM-DD_HHMM}_{STORY-ID}/
│       │       ├── eslint-iter{N}.sarif
│       │       ├── tests-iter{N}.junit.xml
│       │       ├── coverage-final.json
│       │       ├── semgrep-final.sarif
│       │       └── meta.json
│       └── ci/                           # CI-run outputs (BOO-32), gitignored
│           └── run-{github-action-id}/
│               ├── eslint.sarif
│               ├── tests.junit.xml
│               ├── coverage.lcov + coverage.json
│               ├── semgrep.sarif
│               └── sonarqube.json
├── lib/                                  # Library code (Node) / package code (Python)
├── pitch/                                # Pitch briefings (BOO-37)
│   └── PITCH-XX.md                       # One per pitch event; committed
├── specs/                                # Per-story spec files (BOO-23)
│   ├── TEMPLATE.md                       # Spec template with frontmatter
│   └── {ISSUE-PREFIX}XXX.md              # One per story; gates commit
├── sonar-project.properties              # SonarQube config if BOO-5 enabled
└── lighthouserc.json                     # Lighthouse config if BOO-45 enabled
```

## §2. Frontmatter conventions

**Why machine-readable frontmatter:** Schrader Ch. 6 calls for Outcome Governance — measuring outcomes, not output. That only works if the relevant numbers are queryable from a script, not buried in prose. Every artefact below carries the smallest frontmatter that makes it queryable.

### Story spec frontmatter (`specs/{ISSUE-PREFIX}XXX.md`)

Set by `/ideation` step 5b (or manually for stories not created via AI tool):

```yaml
---
story_id: {ISSUE-PREFIX}XXX
estimate: 3                                # Story points (1, 2, 3, 5, 8)
token_estimate: 38000                      # Absolute tokens (BOO-39 heuristic output)
execution_mode: sub-agents                 # linear | sub-agents | agentic
estimation_basis: |
  Prose: how the token estimate was derived
---
```

### Skill frontmatter (`SKILL.md` in any skill)

```yaml
---
name: <skill-name>
version: X.Y.Z
description: <one-line description>
metadata:
  hermes:
    category: <governance | coding | doku | research | trading | personal-assistant>
    tags: [<tag1>, <tag2>, ...]
    requires_toolsets: [<toolset1>, <toolset2>, ...]
    related_skills: [<other-skill>, ...]
---
```

`metadata.hermes` is the Hermes-Bridge (BOO-31). The block is additive — tools that don't know about Hermes ignore it.

### Sprint review frontmatter (`journal/sprint-{date}.md`)

Set by `/sprint-review` step 2b (aggregation of Local + CI + SonarQube + L3):

```yaml
---
sprint: 12
stories: [BOO-15, BOO-16, BOO-17]
metrics:
  eslint_iterations_avg: 2.3
  eslint_recurring_rules: ["no-unused-vars (4x)", ...]
  semgrep_findings_total: 0
  coverage_trend: "82% -> 84% (+2pp)"
  pre_flight_warnings_count: 1            # BOO-40 feedback
  ci_failures_top5: [...]
  sonarqube_hotspots_new: 1
  sonarqube_hotspots_resolved: 3
  sonarqube_cognitive_complexity_trend: "stable"
---
```

### Pitch frontmatter (`pitch/PITCH-XX.md`)

Set by `/pitch` step 5 (BOO-37):

```yaml
---
pitch_id: PITCH-12
sprint: 12
created_at: 2026-04-28T14:00:00Z
related_intents: [INTENT-3, INTENT-5]
related_stories: [BOO-15, BOO-16, BOO-17]
metrics_snapshot:
  loc_delta: "+2,341 / -890"
  coverage_trend: "82% → 84% (+2pp)"
  p95_change: "180ms → 145ms (-19%)"
  iterations_avg: 2.3
  feature_flags_active: 3
  intent_fulfillment_score: 0.85
demo_path: "User-Onboarding → Search → Checkout"
status: prepared | delivered | post-mortem
---
```

### `meta.json` per implement-run

Set by `/implement` step 6f-bis (BOO-36):

```json
{
  "story_id": "BOO-15",
  "started_at": "2026-04-27T14:30:00Z",
  "completed_at": "2026-04-27T14:34:00Z",
  "iterations": {"eslint": 3, "tests": 2, "semgrep": 1, "coverage": 1},
  "final_status": "passed",
  "environment": "mac",
  "pre_flight_warning": null
}
```

## §3. Workflow conventions

**Why these gates exist:** Schrader Ch. 3 frames the move from Vibe Coding to Agentic Engineering as the difference between "the AI typed it" and "the AI typed it AND the code is production-ready". The conventions in this section are the production-readiness floor — without them, the bundle would only support the first half.

### Spec-Gate (BOO-23)

Before any commit that references an issue (e.g. `BOO-42: Add feature`), a spec file `specs/BOO-42.md` must exist. The git hook `hooks/spec-gate.sh` enforces this. Tool-agnostic — runs in any git workflow.

### Doc-Version-Sync

`hooks/doc-version-sync.sh` checks that version strings across CLAUDE.md, ARCHITECTURE_DESIGN.md, GOVERNANCE.md, package.json, and pyproject.toml stay in sync. Tool-agnostic bash.

### Audit-Trail (BOO-19)

Every `/implement` run writes a `## Session-Referenz` block into the spec file (commit-SHA + session-ID + log path + audit-trace.sh command). `bootstrap/scripts/audit-trace.sh` reads the spec and reconstructs the conversation log. Tool-agnostic bash.

### Sensitive-Paths-Gate (BOO-18)

`.claude/sensitive-paths.json` declares mandatory-review patterns (auth/**, payment/**, **/*credential*, etc.). Implementation step 5.5 blocks the run until the operator writes `review-ok: {name} - {comment}`. Tool-agnostic — the JSON config + step semantics work for any AI tool.

### Quality-Gate Architecture (3 layers)

- **Layer 1 — IDE:** Real-time feedback while typing (Error Lens, ESLint plugin, SonarQube for IDE)
- **Layer 2 — CLI / Pre-Commit:** Hard block, locally (`npx eslint`, `semgrep`, dependency check, coverage gate)
- **Layer 3 — CI / GitHub Actions:** Required status check, guards against `--no-verify` bypass

Layers 2 and 3 are tool-agnostic. Layer 1 depends on the editor + AI tool (Claude Code uses IDE plugins; Codex doesn't have IDE integration).

### Learning Loop (L1 / L2 / L3)

| Level | Storage | Recommended when |
|---|---|---|
| L1 | `journal/learnings.md` (markdown, git-tracked) | default |
| L2 | `journal/learnings/{YYYY-QX}.md` (markdown + frontmatter, Dataview-ready) | ~10+ sprints |
| L3 | `journal/learnings.db` (SQLite, gitignored, local analysis) | 50+ sprints |

`/sprint-review` writes (step 7+8), `/ideation` reads (step 0.5). Tool-agnostic — any AI tool can produce the same write+read pattern.

### Sprint Sizing (BOO-38)

Sprint = work that fits in 80% of the current context window without compaction. Model-independent. Story points dual function:

| SP | Sprint-budget share | Tokens @ 200k | Execution mode |
|---|---|---|---|
| 1 | ~5% | ~8k | linear |
| 2 | ~10-15% | ~16-24k | linear / sub-agents |
| 3 | ~20-30% | ~32-48k | sub-agents |
| 5 | ~40-60% | ~64-96k | agentic |
| 8 | >60% | — | must be split |

Token-Window Pre-Flight check (BOO-40): `/implement` step 0b reads current context level + story token-estimate, projects, warns at warn-threshold (70%), recommends sprint-close at hard-threshold (80%). Tool-agnostic concept — implementation depends on which AI tool measures the context.

**Why this convention exists:** Schrader Ch. 2 attacks Velocity as the wrong metric — it measures output, not outcome, and incentivises cargo-cult ritual. Replacing the time-box with a token-box (the natural unit of AI cost) and replacing burndown with Intent-fulfilment scoring removes the bad incentive at the source.

### 4P-Pipeline order (BOO-37 + Appendix L)

`/intent → /ideation → /backlog → /implement → /architecture-review → /sprint-review → /pitch`

Read end-to-end: `/intent` captures the *Perceive* phase, `/ideation` + `/backlog` are *Prompt*, `/implement` + `/architecture-review` + `/sprint-review` are *Produce*, `/pitch` is *Pitch*. Skipping a phase is allowed only when the artefact it produces already exists — e.g. you can skip `/intent` if `intents/INTENT-X.md` was created elsewhere.

## §4. Tool-agnostic components (run with any AI tool, or with none)

These components run as plain bash / pure files — they don't depend on Claude Code at all:

- **bash hooks** — `spec-gate.sh`, `doc-version-sync.sh`, `audit-trace.sh`, `setup-branch-protection.sh`, `dependency-check.sh`, `runner-healthcheck.sh`
- **GitHub Actions** — all workflows in `.github/workflows/`
- **Configuration files** — `.claude/environment.json`, `.claude/sensitive-paths.json`, `sonar-project.properties`, `lighthouserc.json`
- **Markdown artifacts** — `CLAUDE.md`, `ARCHITECTURE_DESIGN.md`, `GOVERNANCE.md`, `SECURITY.md`, `specs/TEMPLATE.md`, `intents/INTENT-XX.md`, `pitch/PITCH-XX.md`, `journal/learnings.md`
- **Migration tooling** — `bootstrap/scripts/migrate-to-v2.sh` with all `migrate_boo_XX()` functions
- **`journal/`-tree convention** — all reports + learnings layouts

## §5. Tool-specific components (vary per AI tool)

These differ depending on the AI coding tool in use:

- **Skill invocation** — Claude Code: slash commands `/bootstrap`, `/implement`. Codex: `@Codex` in Linear issue or direct CLI. Cursor: `.cursorrules` + manual prompt.
- **Skill location** — Claude Code: `~/.claude/skills/<name>/SKILL.md`. Codex: `.codex/skills/<name>/SKILL.md` (same format). Cursor: maps via `.cursorrules`.
- **MCP integrations** — Anthropic-specific; Codex needs context-bridges via `CLAUDE.md` or n8n; other tools usually have no MCP.
- **Sub-agent / parallel execution** — Claude Code has built-in sub-agents; Codex achieves this via parallel worktrees + TOML automations.

For detailed adapter guidance per tool, see HANDBUCH **Appendix K — Tool-Adapter**.

## §6. Tool-portability checklist

A project follows the Code-Crash Framework conventions (and is therefore tool-portable) when:

- [ ] Directory layout matches §1 (including `intents/` and `pitch/` since bootstrap v3.23.0)
- [ ] `specs/TEMPLATE.md` exists with the frontmatter from §2
- [ ] `hooks/spec-gate.sh` is installed in `.git/hooks/pre-commit`
- [ ] `.github/workflows/` has the CI gates relevant for the stack
- [ ] `journal/` exists (at minimum with `learnings.md` for L1)
- [ ] `.claude/environment.json` exists with paths + tools_available
- [ ] `CLAUDE.md` documents the project state (used by Claude Code AND Codex)
- [ ] `metadata.hermes` block in skill frontmatter (only relevant if Hermes-Bridge active)

If yes → operator can switch AI coding tools (Claude Code → Codex → Cursor → local LLM) without re-bootstrapping. The framework itself is unchanged.

## Related documents

- `HANDBUCH.md` — full user guide including §1-12 + Appendix A-M
- `HANDBUCH.md` Appendix K — Tool-Adapter (how to use this framework with Codex, Cursor, Aider, local LLMs)
- `HANDBUCH.md` Appendix L — 4P pipeline mapping (Pitch as the closing phase)
- `HANDBUCH.md` Appendix M — Schrader Decoder (chapter-by-chapter mapping into the bundle)
- `bootstrap/references/file-templates.md` — all file templates (DE+EN)
- `bootstrap/references/migration-checklist-v1-to-v2.md` — per-issue migration paths (DE+EN)
- ADR `2026-04-22 Schrader-Code-Crash-Integration` — original framework rationale
- Brainstorming 2026-05-12 — Tool-Unabhängigkeit als strategische Initiative (BOO-49)

---

<a name="deutsch"></a>

# Code-Crash Framework — Konventionen

> **Was das hier ist.** Eine tool-neutrale Spezifikation des Code-Crash Frameworks: die Datei-Layouts, Frontmatter-Schemata, Workflow-Regeln und CI-Gates, die jedes Bundle-konforme Projekt einhaelt — unabhaengig vom verwendeten KI-Coding-Tool (Claude Code, Codex, Cursor, Aider, lokales LLM, oder gar kein KI-Tool).
>
> **Warum es das gibt.** Das Framework ist die operative Antwort auf Matthias Schraders "Code Crash" (2026): sobald KI das Code-Schreiben uebernimmt, wandert der Engpass vom Tippen zum Intent — und die Arbeit muss um diesen neuen Engpass herum neu organisiert werden. Die Konventionen in diesem Dokument sind genau die Bausteine, die das real machen. Sie sind tool-portabel, weil sich die KI-Landschaft schneller bewegt als irgendein einzelnes Tool optimal bleibt. Fuer das Kapitel-fuer-Kapitel-Mapping von Schraders Buch in dieses Bundle siehe **HANDBUCH Anhang M — Schrader-Decoder**.

## §0. Fuer wen ist dieses Dokument

Du bist wahrscheinlich aus einem von drei Gruenden hier. Das Dokument ist so geschrieben, dass jeder Leser schnell die richtige Stelle findet.

### Als Operator beim Aufsetzen eines neuen Projekts

Lies zuerst §1 (Projekt-Layout) und §6 (Tool-Portabilitaets-Checkliste). Alles, was du brauchst, um ein konformes Repository zu scaffolden, steht in diesen zwei Sektionen. Die anderen Sektionen erklaeren das *warum* — nuetzlich, wenn du bewusst abweichen willst.

### Als Operator beim Wechsel weg von Claude Code (oder rauf auf Claude Code)

Lies zuerst §4 (tool-agnostische Komponenten) und §5 (tool-spezifische Komponenten), dann §6. Etwa 70 % des Frameworks — Hooks, GitHub Actions, Markdown-Artefakte, der `journal/`-Baum — laufen unveraendert, egal welches KI-Tool du nutzt. Die restlichen 30 % (Skill-Aufruf, MCP-Integrationen, Sub-Agents) verdrahtest du pro Tool neu. **HANDBUCH Anhang K — Tool-Adapter** fuehrt durch den Wechsel Schritt fuer Schritt.

### Als jemand, der das Framework verstehen will

Lies §0 (diese Sektion), dann §3 (Workflow-Konventionen), dann **HANDBUCH Anhang M — Schrader-Decoder**. Die Konventionen in §1-§2 sind groesstenteils mechanisch; das *warum* lebt in §3 und im Schrader-Decoder. Wenn du Schraders Buch nie aufgemacht hast, ist das in Ordnung — der Decoder ist eine Uebersetzung, kein Re-Reading.

### Glossar (die acht Begriffe, die du vor allem anderen brauchst)

| Begriff | Ein-Satz-Definition |
|---|---|
| **Intent** | Das praezise, messbare Ergebnis, das ein Nutzer erreichen soll — die neue knappe Ressource, seit KI die Kosten des Code-Schreibens absorbiert hat (Schrader Kap. 4). |
| **4P-Pipeline** | Perceive → Prompt → Produce → Pitch. Der End-to-End-Fluss vom wahrgenommenen Problem zum ausgelieferten Demo (Schrader Kap. 5). Jede Phase ist an einen oder mehrere Skills gebunden (Anhang L). |
| **Skill** | Ein wiederholbarer Workflow, den Claude Code (oder ein anderes KI-Tool) bei Aufruf via Slash-Command ausfuehrt — `/intent`, `/implement`, `/sprint-review`, etc. Liegt unter `<skill>/SKILL.md`. |
| **BOO-ID** | Linear-Issue-Identifier im Projekt "Bootstrapping Evolution". Jede Framework-Aenderung hat eine BOO-ID — die Konventionen verweisen darauf, damit du jede Regel zu ihrem Ursprung zurueckverfolgen kannst. |
| **Spec-File** | `specs/{ISSUE-ID}.md` pro Story. Erzeugt von `/ideation`, blockiert durch `hooks/spec-gate.sh`, audit-trailed von `/implement`. Die kleinste Einheit von "was wir gerade bauen". |
| **Quality-Gate** | Jede Pruefung, die durchlaufen muss, bevor Code weiter darf — ESLint, Semgrep, Coverage, Performance-Baseline, SonarQube. In drei Layern verdrahtet (IDE, Pre-Commit, CI). |
| **Learning-Loop** | L1 / L2 / L3 — Markdown, datiertes Markdown, SQLite. Das Framework schreibt Lessons nach jedem Sprint und liest sie vor jeder Story, damit derselbe Fehler beim zweiten Mal weniger kostet. |
| **Hermes** | Optionaler externer Compound-Engineering-Layer, der die CI-Outputs des Bundles konsumiert und projekt-uebergreifende Muster vorschlaegt. Lebt auf einer eigenen VPS. Auslassen ist voll unterstuetzt. |

## §1. Projekt-Layout

Jedes Projekt, das dem Code-Crash Framework folgt, hat diese Top-Level-Struktur. **Warum dieses Layout existiert:** Schrader Kap. 5 argumentiert, dass die 4P-Pipeline den klassischen Genehmigungsprozess ersetzt — das funktioniert nur, wenn Intent (`intents/`), Ausfuehrung (`specs/`), Evidenz (`journal/`) und Outcome (`pitch/`) jeweils einen festen Platz haben.

```
<project-root>/
├── .claude/                              # Tool-Konfig (Claude Code; andere Tools spiegeln)
│   ├── environment.json                  # Pfade, Schwellwerte, tools_available
│   ├── sensitive-paths.json              # Pflicht-Review-Muster (BOO-18)
│   └── ISSUE_WRITING_GUIDELINES.md       # Story-Template-Regeln (BOO-11/27/30)
├── .github/
│   └── workflows/                        # CI-Gates (BOO-3/4/5/12/15/16/28/45)
│       ├── eslint.yml                    # oder ruff.yml fuer Python
│       ├── semgrep.yml
│       ├── sonar.yml                     # nur wenn SonarQube aktiv (BOO-5)
│       ├── perf.yml                      # Backend-Stacks (BOO-16)
│       ├── lighthouse.yml                # Frontend-Stacks (BOO-45)
│       └── ISSUE_TEMPLATE/
│           └── story.yml                 # 4-Schrader-Block-Template (BOO-11/27/30)
├── ARCHITECTURE_DESIGN.md                # §1-9 Hub-Doku, Single Source of Truth
├── CHANGELOG.md
├── CLAUDE.md                             # Projekt-Kontext fuer KI-Tools (auch fuer Codex)
├── GOVERNANCE.md                         # Projekt-spezifische Regeln und Tool-Stack
├── Projekt-Governance.md                 # SecondBrain-seitige Governance (wenn Obsidian aktiv)
├── README.md
├── SECURITY.md
├── bench/                                # Benchmarks pro Service (BOO-16)
│   └── <service>.bench.js oder _bench.py
├── docs/                                 # Architektur-Diagramme, ADRs
├── hooks/                                # Lokale Git-Hooks (NICHT .git/hooks/)
│   ├── spec-gate.sh                      # BOO-23: jeder Commit braucht eine Spec
│   ├── doc-version-sync.sh               # Versions-Sync ueber Doku
│   └── dependency-check.sh               # BOO-12: Halluzinations-Check
├── intents/                              # Intent-Files (BOO-1)
│   └── INTENT-XX.md                      # Einer pro Intent; gelesen von /pitch
├── journal/
│   ├── learnings.md                      # L1 (Default)
│   ├── learnings/{YYYY-QX}.md            # L2 (empfohlen ab ~10 Sprints)
│   ├── learnings.db                      # L3 (empfohlen ab 50+ Sprints, gitignored)
│   ├── perf-baseline.json                # Lebende Performance-Baseline (BOO-16)
│   └── reports/
│       ├── local/                        # Implement-Run-Outputs (BOO-36), gitignored
│       │   └── {YYYY-MM-DD_HHMM}_{STORY-ID}/
│       │       ├── eslint-iter{N}.sarif
│       │       ├── tests-iter{N}.junit.xml
│       │       ├── coverage-final.json
│       │       ├── semgrep-final.sarif
│       │       └── meta.json
│       └── ci/                           # CI-Run-Outputs (BOO-32), gitignored
│           └── run-{github-action-id}/
│               ├── eslint.sarif
│               ├── tests.junit.xml
│               ├── coverage.lcov + coverage.json
│               ├── semgrep.sarif
│               └── sonarqube.json
├── lib/                                  # Library-Code (Node) / Package-Code (Python)
├── pitch/                                # Pitch-Briefings (BOO-37)
│   └── PITCH-XX.md                       # Eines pro Pitch-Event; committed
├── specs/                                # Pro-Story Spec-Files (BOO-23)
│   ├── TEMPLATE.md                       # Spec-Template mit Frontmatter
│   └── {ISSUE-PREFIX}XXX.md              # Eines pro Story; gated den Commit
├── sonar-project.properties              # SonarQube-Config falls BOO-5 aktiv
└── lighthouserc.json                     # Lighthouse-Config falls BOO-45 aktiv
```

## §2. Frontmatter-Konventionen

**Warum maschinenlesbares Frontmatter:** Schrader Kap. 6 verlangt Outcome Governance — Outcomes messen, nicht Output. Das geht nur, wenn die relevanten Zahlen von einem Script abfragbar sind, nicht in Prosa vergraben. Jedes Artefakt unten traegt das kleinstmoegliche Frontmatter, das es abfragbar macht.

### Story-Spec-Frontmatter (`specs/{ISSUE-PREFIX}XXX.md`)

Gesetzt von `/ideation` Schritt 5b (oder manuell fuer Stories, die nicht via KI-Tool entstehen):

```yaml
---
story_id: {ISSUE-PREFIX}XXX
estimate: 3                                # Story Points (1, 2, 3, 5, 8)
token_estimate: 38000                      # Absolute Tokens (BOO-39-Heuristik-Output)
execution_mode: sub-agents                 # linear | sub-agents | agentic
estimation_basis: |
  Prosa: wie das Token-Estimate hergeleitet wurde
---
```

### Skill-Frontmatter (`SKILL.md` in jedem Skill)

```yaml
---
name: <skill-name>
version: X.Y.Z
description: <ein-Satz-Beschreibung>
metadata:
  hermes:
    category: <governance | coding | doku | research | trading | personal-assistant>
    tags: [<tag1>, <tag2>, ...]
    requires_toolsets: [<toolset1>, <toolset2>, ...]
    related_skills: [<other-skill>, ...]
---
```

`metadata.hermes` ist die Hermes-Bruecke (BOO-31). Der Block ist additiv — Tools, die Hermes nicht kennen, ignorieren ihn.

### Sprint-Review-Frontmatter (`journal/sprint-{date}.md`)

Gesetzt von `/sprint-review` Schritt 2b (Aggregation aus Local + CI + SonarQube + L3):

```yaml
---
sprint: 12
stories: [BOO-15, BOO-16, BOO-17]
metrics:
  eslint_iterations_avg: 2.3
  eslint_recurring_rules: ["no-unused-vars (4x)", ...]
  semgrep_findings_total: 0
  coverage_trend: "82% -> 84% (+2pp)"
  pre_flight_warnings_count: 1            # BOO-40 Feedback
  ci_failures_top5: [...]
  sonarqube_hotspots_new: 1
  sonarqube_hotspots_resolved: 3
  sonarqube_cognitive_complexity_trend: "stable"
---
```

### Pitch-Frontmatter (`pitch/PITCH-XX.md`)

Gesetzt von `/pitch` Schritt 5 (BOO-37):

```yaml
---
pitch_id: PITCH-12
sprint: 12
created_at: 2026-04-28T14:00:00Z
related_intents: [INTENT-3, INTENT-5]
related_stories: [BOO-15, BOO-16, BOO-17]
metrics_snapshot:
  loc_delta: "+2,341 / -890"
  coverage_trend: "82% → 84% (+2pp)"
  p95_change: "180ms → 145ms (-19%)"
  iterations_avg: 2.3
  feature_flags_active: 3
  intent_fulfillment_score: 0.85
demo_path: "User-Onboarding → Search → Checkout"
status: prepared | delivered | post-mortem
---
```

### `meta.json` pro Implement-Run

Gesetzt von `/implement` Schritt 6f-bis (BOO-36):

```json
{
  "story_id": "BOO-15",
  "started_at": "2026-04-27T14:30:00Z",
  "completed_at": "2026-04-27T14:34:00Z",
  "iterations": {"eslint": 3, "tests": 2, "semgrep": 1, "coverage": 1},
  "final_status": "passed",
  "environment": "mac",
  "pre_flight_warning": null
}
```

## §3. Workflow-Konventionen

**Warum diese Gates existieren:** Schrader Kap. 3 beschreibt den Sprung von Vibe Coding zu Agentic Engineering als Unterschied zwischen "die KI hat es getippt" und "die KI hat es getippt UND der Code ist produktionsreif". Die Konventionen in diesem Kapitel sind die Produktionsreife-Untergrenze — ohne sie wuerde das Bundle nur die erste Haelfte abdecken.

### Spec-Gate (BOO-23)

Vor jedem Commit, der eine Issue-ID referenziert (z.B. `BOO-42: Add feature`), muss die Datei `specs/BOO-42.md` existieren. Der Git-Hook `hooks/spec-gate.sh` setzt das durch. Tool-agnostisch — laeuft in jedem Git-Workflow.

### Doc-Version-Sync

`hooks/doc-version-sync.sh` prueft, ob Versions-Strings ueber CLAUDE.md, ARCHITECTURE_DESIGN.md, GOVERNANCE.md, package.json und pyproject.toml synchron sind. Tool-agnostisches Bash.

### Audit-Trail (BOO-19)

Jeder `/implement`-Lauf schreibt einen `## Session-Referenz`-Block ins Spec-File (Commit-SHA + Session-ID + Log-Pfad + `audit-trace.sh`-Command). `bootstrap/scripts/audit-trace.sh` liest das Spec und rekonstruiert das Konversations-Log. Tool-agnostisches Bash.

### Sensitive-Paths-Gate (BOO-18)

`.claude/sensitive-paths.json` deklariert Pflicht-Review-Muster (auth/**, payment/**, **/*credential*, etc.). Implement-Schritt 5.5 blockiert den Lauf, bis der Operator `review-ok: {name} - {comment}` schreibt. Tool-agnostisch — die JSON-Config + Schritt-Semantik funktionieren mit jedem KI-Tool.

### Quality-Gate-Architektur (3 Layer)

- **Layer 1 — IDE:** Echtzeit-Feedback waehrend des Tippens (Error Lens, ESLint-Plugin, SonarQube for IDE)
- **Layer 2 — CLI / Pre-Commit:** Hard Block, lokal (`npx eslint`, `semgrep`, Dependency-Check, Coverage-Gate)
- **Layer 3 — CI / GitHub Actions:** Required Status Check, schuetzt gegen `--no-verify`-Umgehung

Layer 2 und 3 sind tool-agnostisch. Layer 1 haengt vom Editor + KI-Tool ab (Claude Code nutzt IDE-Plugins; Codex hat keine IDE-Integration).

### Learning-Loop (L1 / L2 / L3)

| Level | Speicher | Empfohlen wenn |
|---|---|---|
| L1 | `journal/learnings.md` (Markdown, git-tracked) | Default |
| L2 | `journal/learnings/{YYYY-QX}.md` (Markdown + Frontmatter, Dataview-ready) | ~10+ Sprints |
| L3 | `journal/learnings.db` (SQLite, gitignored, lokale Analyse) | 50+ Sprints |

`/sprint-review` schreibt (Schritt 7+8), `/ideation` liest (Schritt 0.5). Tool-agnostisch — jedes KI-Tool kann das gleiche Schreib+Lese-Muster produzieren.

### Sprint-Sizing (BOO-38)

Sprint = Arbeit, die in 80 % des aktuellen Context-Windows passt, ohne Compaction. Modell-unabhaengig. Story Points haben duale Funktion:

| SP | Sprint-Budget-Anteil | Tokens @ 200k | Ausfuehrungs-Modus |
|---|---|---|---|
| 1 | ~5% | ~8k | linear |
| 2 | ~10-15% | ~16-24k | linear / sub-agents |
| 3 | ~20-30% | ~32-48k | sub-agents |
| 5 | ~40-60% | ~64-96k | agentic |
| 8 | >60% | — | muss gesplittet werden |

Token-Window-Pre-Flight (BOO-40): `/implement` Schritt 0b liest aktuelles Context-Level + Token-Estimate der Story, projiziert, warnt bei warn-threshold (70%), empfiehlt Sprint-Close bei hard-threshold (80%). Tool-agnostisches Konzept — Implementation haengt davon ab, welches KI-Tool das Context-Window misst.

**Warum diese Konvention existiert:** Schrader Kap. 2 greift Velocity als falsche Metrik an — sie misst Output statt Outcome und produziert Cargo-Cult-Ritualisierung. Die Zeit-Box durch eine Token-Box (die natuerliche Einheit von KI-Kosten) zu ersetzen und Burndown durch Intent-Erfuellungs-Score zu ersetzen, entfernt den falschen Anreiz an der Wurzel.

### 4P-Pipeline-Reihenfolge (BOO-37 + Anhang L)

`/intent → /ideation → /backlog → /implement → /architecture-review → /sprint-review → /pitch`

End-to-End gelesen: `/intent` deckt die *Perceive*-Phase ab, `/ideation` + `/backlog` sind *Prompt*, `/implement` + `/architecture-review` + `/sprint-review` sind *Produce*, `/pitch` ist *Pitch*. Eine Phase ueberspringen ist nur erlaubt, wenn das Artefakt, das sie produziert, schon existiert — z.B. `/intent` darf entfallen, wenn `intents/INTENT-X.md` anderswo entstanden ist.

## §4. Tool-agnostische Komponenten (laufen mit jedem KI-Tool oder mit keinem)

Diese Komponenten laufen als plain Bash / pure Files — sie haengen ueberhaupt nicht von Claude Code ab:

- **Bash-Hooks** — `spec-gate.sh`, `doc-version-sync.sh`, `audit-trace.sh`, `setup-branch-protection.sh`, `dependency-check.sh`, `runner-healthcheck.sh`
- **GitHub Actions** — alle Workflows in `.github/workflows/`
- **Konfigurations-Files** — `.claude/environment.json`, `.claude/sensitive-paths.json`, `sonar-project.properties`, `lighthouserc.json`
- **Markdown-Artefakte** — `CLAUDE.md`, `ARCHITECTURE_DESIGN.md`, `GOVERNANCE.md`, `SECURITY.md`, `specs/TEMPLATE.md`, `intents/INTENT-XX.md`, `pitch/PITCH-XX.md`, `journal/learnings.md`
- **Migrations-Tooling** — `bootstrap/scripts/migrate-to-v2.sh` mit allen `migrate_boo_XX()`-Funktionen
- **`journal/`-Baum-Konvention** — alle Reports- + Learnings-Layouts

## §5. Tool-spezifische Komponenten (variieren pro KI-Tool)

Diese unterscheiden sich je nach KI-Coding-Tool:

- **Skill-Aufruf** — Claude Code: Slash-Commands `/bootstrap`, `/implement`. Codex: `@Codex` im Linear-Issue oder direkter CLI-Call. Cursor: `.cursorrules` + manueller Prompt.
- **Skill-Ablage** — Claude Code: `~/.claude/skills/<name>/SKILL.md`. Codex: `.codex/skills/<name>/SKILL.md` (gleiches Format). Cursor: Mapping via `.cursorrules`.
- **MCP-Integrationen** — Anthropic-spezifisch; Codex braucht Kontext-Bruecken via `CLAUDE.md` oder n8n; andere Tools haben meist kein MCP.
- **Sub-Agent / Parallel-Ausfuehrung** — Claude Code hat eingebaute Sub-Agents; Codex erreicht das via parallele Worktrees + TOML-Automationen.

Fuer detaillierte Adapter-Anleitung pro Tool siehe HANDBUCH **Anhang K — Tool-Adapter**.

## §6. Tool-Portabilitaets-Checkliste

Ein Projekt folgt den Code-Crash-Framework-Konventionen (und ist damit tool-portabel), wenn:

- [ ] Verzeichnis-Layout matched §1 (inkl. `intents/` und `pitch/` seit Bootstrap v3.23.0)
- [ ] `specs/TEMPLATE.md` existiert mit dem Frontmatter aus §2
- [ ] `hooks/spec-gate.sh` ist in `.git/hooks/pre-commit` installiert
- [ ] `.github/workflows/` hat die fuer den Stack relevanten CI-Gates
- [ ] `journal/` existiert (mindestens mit `learnings.md` fuer L1)
- [ ] `.claude/environment.json` existiert mit Pfaden + tools_available
- [ ] `CLAUDE.md` dokumentiert den Projekt-Stand (genutzt von Claude Code UND Codex)
- [ ] `metadata.hermes`-Block in Skill-Frontmatter (nur relevant, wenn Hermes-Bruecke aktiv)

Wenn ja → Operator kann KI-Coding-Tools wechseln (Claude Code → Codex → Cursor → lokales LLM), ohne neu zu bootstrappen. Das Framework selbst bleibt unveraendert.

## Verwandte Dokumente

- `HANDBUCH.md` — vollstaendiges Handbuch inkl. §1-12 + Anhang A-M
- `HANDBUCH.md` Anhang K — Tool-Adapter (wie man das Framework mit Codex, Cursor, Aider, lokalen LLMs nutzt)
- `HANDBUCH.md` Anhang L — 4P-Pipeline-Mapping (Pitch als geschlossene Phase)
- `HANDBUCH.md` Anhang M — Schrader-Decoder (Kapitel-fuer-Kapitel-Mapping ins Bundle)
- `bootstrap/references/file-templates.md` — alle File-Templates (DE+EN)
- `bootstrap/references/migration-checklist-v1-to-v2.md` — pro-Issue-Migrationspfade (DE+EN)
- ADR `2026-04-22 Schrader-Code-Crash-Integration` — urspruengliche Framework-Begruendung
- Brainstorming 2026-05-12 — Tool-Unabhaengigkeit als strategische Initiative (BOO-49)
